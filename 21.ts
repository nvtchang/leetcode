var list1 = [1,2,4]
var list2 = [1,3,4]

if(list1.length && list2.length) {
    const merge = list1.concat(list2).sort()
    console.log("merge", merge)
} else if(list1.length! && list2.length!) {

} 
else {
    
}